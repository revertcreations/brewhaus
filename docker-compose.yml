services:
  server:
    build: ./server
    expose:
      - "4000"
    environment:
      - PORT=4000

  client:
    build:
      context: ./client
      args:
        VITE_GRAPHQL_URL: ${GRAPHQL_URL:-http://localhost:4000}
    expose:
      - "80"
    depends_on:
      - server
