services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    environment:
      - PORT=4000

  client:
    build:
      context: ./client
      args:
        VITE_GRAPHQL_URL: ${GRAPHQL_URL:-http://localhost:4000}
    ports:
      - "80:80"
    depends_on:
      - server
